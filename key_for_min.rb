# prereqs: iterators, hashes, conditional logic
# Given a hash with numeric values, return the key for the smallest value

def key_for_min_value(hash)
hash.each do |name, min_number|
  i = 0
 while i < hash.length 
  if min_num < num
end

def key_for_min_value(hash)
  min_key =
  min_value =
hash.each do |name, number|
if number < min_value
  
    
  end
  end
end
